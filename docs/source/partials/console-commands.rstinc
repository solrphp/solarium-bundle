console commands
----------------
| in order to use these commands, make sure you've got at least one :doc:`endpoint <../reference/configuration/endpoints>` and :doc:`client <../reference/configuration/endpoints>` configured.

.. tip::
    | use the verbosity options (`-v|vv|vvv`) for displaying more verbose errors once they occur.

| currently supported features and commands: